<h2 class="profiles-listing">All Profiles</h2>

<table>
  <tr>
    <th>Member name</th>
    <th>Member since</th>
  </tr>


<% @profiles.each do |profile| %>
  <tr>
    <td><%= link_to profile.firstname + " " + profile.lastname, profile %></td>
    <td><%= profile.created_at.strftime("%B %e, %Y") %></td>
  </tr>
<% end %>
</table>

<br />