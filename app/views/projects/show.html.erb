
<% title @project.name %>


  <% flash.each do |key, value| %>
    <%= content_tag :div, value, :class => "flash #{key}" unless value.blank? %>
  <% end %>


  <%= render 'profiles/profile_header' %>


<section>
<div class="project">


  <h1 class="name fn">
    <%= @project.name %>
  </h1>
  

  <div class="summary">
    <span class="label">Summary:</span>
    <%= @project.summary %>
  </div>

  <div class="dates">
    <span class="label">Dates:</span>
    <time class="start-date" datetime="<%= @project.startdate.strftime("%Y-%m-%d") %>"><%= @project.startdate.strftime("%B %Y") %></time>
    <em>to </em>
    <time class="end-date" datetime="<%= @project.startdate.strftime("%Y-%m-%d") %>"><%= @project.finishdate.strftime("%B %Y") %></time>
  </div>

  <div class="description">
    <span class="label">Description:</span>
    <%= @project.description.html_safe %>
  </div>

  <% if @project.requiredskills %>
  <div class="requiredskills">
    <span class="label">Required skills:</span>
    <%= @project.requiredskills.html_safe unless @project.requiredskills.blank? %>
  </div>
  <% end %>

  <% if @project.client %>
  <div class="client">
    <span class="label">Client:</span>
    <%= @project.client %>
  </div>
  <% end %>


  <% if @project.uri %>
  <div class="uri">
    <span class="label">Link to project:</span>
    <%= link_to @project.uri_anchor, @project.uri %>
  </div>
  <% end %>

  <% if @project.pointofcontact %>
  <div class="point-of-contact">
    <span class="label">Point of contact:</span>
    <%= @project.pointofcontact %>
  </div>
  <% end %>

  <div class="tags">
    <span class="label">Tags:</span>
    <%= @project.tags %>
  </div>

  <%= render 'project_links' %>

</div><!-- class=project -->
</section>

